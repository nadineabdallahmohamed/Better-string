package Better_string;

public class Second {
 public static String  betterstring (String first,String second,BetterString obj) {
	 
	 if(obj.try_string(first , second))
		 return first;
	 return second;
 }
}
