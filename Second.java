package Better_string;

public class Second {
 public static String  betterstring (String first,String second,Better_str obj) {
	 
	 if(obj.try_string(first , second))
		 return first;
	 return second;
 }
}
